# Friction_Iden（E1 腿关节摩擦辨识）

- **入口脚本**：`run_full_iden_three_files.m`、`run_generate_urdf_validation_trajectory.m`；示例见 `example_*.m`
- **data/**：采集的关节 CSV、测试数据（如 test_data）
- **build/**：辨识结果、生成的轨迹等
- 摩擦辨识原理见仓库根目录（本仓库暂无独立 friction 算法目录时，算法在应用脚本内或可后续抽离）。
